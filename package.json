{
  "name": "pigtrack-pro",
  "private": true,
  "scripts": {
    "sync:open-app": "node scripts/sync-open-app.cjs",
    "dev": "node scripts/dev.cjs",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "node scripts/sync-open-app.cjs && npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
